# Playwright Healer proposal — not auto-applied

Generated by Claude Code `playwright-test-healer` with read-only tools on 2026-07-21.

## Root cause

`CandidateDialog.submit` awaits the request and `onCreated` closes the dialog, unmounting Save before the success toast. The raw draft's post-response `toBeDisabled()` targets a detached element and can fail/race; this is a test defect, not evidence of a product defect.

## Proposed repair (NEEDS HUMAN REVIEW)

1. Count POST requests to `/candidates` before the double-click.
2. Keep the success status assertion.
3. Assert one create request and one resulting row.
4. Remove only the invalid transient-state assertion; do not weaken the persistence assertion, add waits/skips or change baselines.

Risk: exactly one POST tests client double-submit protection, while a legitimate lost-response retry can produce two POSTs with the same idempotency key and one record. Keep API/Hurl idempotency assertions for that separate contract.

No production/test file, assertion, skip, baseline, branch or merge was modified by the healer. A human applied and validated the reviewed test separately.

## Additional human finding during execution

The first reviewed run found two simultaneous `role=status` nodes (candidate loading plus toast). Playwright strict mode correctly rejected the ambiguous locator. Evidence is preserved in `reports/generated/demos/e2e-failure`; the human repair scoped the locator by toast text and retained the request-count and single-row assertions.

The second run exposed the proposal's documented URL risk: the browser calls `/api/candidates`, not `/candidates`. The human review changed the counter to a suffix match; assertion semantics remain exactly one POST and one row.
