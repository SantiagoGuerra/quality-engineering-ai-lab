# Legacy command compatibility

Claude Code now merges custom commands into Agent Skills. The project commands are implemented as `.claude/skills/<command>/SKILL.md`, which creates the same `/command` invocation and supports referenced files and subagents.
